import { Component } from '@angular/core';

@Component({
    selector    : 'app-server',
    templateUrl : './server.component.html',
    // template    : `<div><p> New server here!!!...</p></div>`,
    styleUrls   : ['./server.component.css']
    // styles      : [`p { color:blueviolet}`]
})
export class ServerComponent{
    name="default"
}